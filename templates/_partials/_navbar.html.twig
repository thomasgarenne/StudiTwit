<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_post') }}">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      {% if app.user %}  
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/post/new">Publier</a>
        </li>
        {% endif %}
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        {% if app.user %}
        <li>
        <a class="nav-link" href="{{path('logout')}}">Se déconnecter</a> 
        </li>
        {% else %}
        <li>
          <a class="nav-link" href="{{path('login')}}">Se connecter</a>
        </li>
        {% endif %}
      </ul>
     </div>
  </div>
</nav>